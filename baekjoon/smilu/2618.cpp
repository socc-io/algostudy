#include<iostream>
#include<queue>
#include<map>
using namespace std;

typedef pair<int,int> pii;
typedef long long     lld;

int n, w;
pii pos[1000];

int main()
{
	scanf("%d%d", &n, &w);
	for(int i=0; i<n; ++i) {
		scanf("%d%d", &(pos[i].first), &(pos[i].second));
	}

}


